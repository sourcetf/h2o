- [x] 代码中不存在未处理的 `unwrap()`、`expect()` 以及其他可能引发 panic 的脆弱逻辑。
- [x] 代码中不存在潜在的空指针解引用和数组/缓冲区溢出风险。
- [x] 已成功生成基于 P-384 曲线和 SHA-384 算法的测试证书，并应用到服务器配置中。
- [x] `proxy-core` 和 `h2o` 均在 HTTP/1.1、HTTP/2 和 HTTP/3 下完成了基于该证书的基准测试。
- [x] 提供了详细的压测报告，包含吞吐量、并发性能以及系统资源（CPU/内存）占用对比。